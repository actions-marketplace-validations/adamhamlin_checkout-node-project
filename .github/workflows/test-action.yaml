on: [workflow_dispatch]

jobs:
  test_action:
    runs-on: ubuntu-latest
    name: Workflow to test checkout-node-project action using `act`
    steps:
      - name: Test the checkout-node-project action
        uses: . # uses the action in the repository root
        with:
          node-version: 24.x
